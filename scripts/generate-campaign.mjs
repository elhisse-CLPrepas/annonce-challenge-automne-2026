import { readFileSync, mkdirSync, writeFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { trackedUrl } from '../src/links.js';

const config = JSON.parse(readFileSync(new URL('../config/campaign.json', import.meta.url), 'utf8'));
const destination = config.announcementUrl || config.offerUrl;
const out = new URL('../campagne/', import.meta.url);
mkdirSync(out, { recursive: true });
const urls = Object.fromEntries(config.sources.map((source) => [source, trackedUrl(destination, config, { source })]));
const messages = {
  whatsapp: `RIEN NE REMPLACE LA PRATIQUE.\n\nCHALLENGE 100 JOURS — RELANCE AUTOMNE 2026\nDépart le vendredi 25 septembre 2026.\n\nVous avez un projet ? Apprenez à le cadrer, à piloter l’IA, à produire, à vérifier et à documenter vos résultats dans un portfolio.\n\nAvec Codex, VS Code, Git et GitHub, la méthode se construit dans vos réalisations.\n\nDécouvrez la formation et présentez votre besoin :\n${urls.whatsapp}\n\nL’IA assiste. L’humain pilote, contrôle et décide.\nProf. Abderrahman EL HISSE — LAB-NUMÉRIQUE-IA`,
  facebook: `RIEN NE REMPLACE LA PRATIQUE.\n\nInstaller une pratique qui dure demande une vision, une méthode et de la régularité.\n\nCHALLENGE 100 JOURS — RELANCE AUTOMNE 2026\nDépart le 25 septembre 2026.\n\nEnseignants, formateurs, coachs, entrepreneurs et professionnels : quel besoin de votre activité souhaitez-vous transformer en première réalisation concrète ?\n\nUn support pédagogique, une page d’offre, un dossier projet ou une procédure : vous partez d’un besoin réel pour produire, tester, corriger et expliquer votre travail.\n\nVotre métier. Votre projet. Vos productions.\n\nDécouvrez le parcours et présentez votre besoin :\n${urls.facebook}\n\nApprendre en produisant. Produire avec méthode. Partager avec valeur. Avancer avec conscience.\n\nProf. Abderrahman EL HISSE — LAB-NUMÉRIQUE-IA\n\n#Challenge100Jours #FormationIA #ApprendreEnPratiquant #AutonomieNumerique #LNIA`,
  linkedin: `RIEN NE REMPLACE LA PRATIQUE.\n\nLes outils évoluent. Votre progression se construit dans les projets que vous réalisez, les erreurs que vous corrigez et les résultats que vous savez expliquer et reproduire.\n\nC’est le sens du Challenge 100 Jours du LAB-NUMÉRIQUE-IA : apprendre à produire avec l’intelligence artificielle pour gagner en autonomie dans votre activité.\n\nRELANCE AUTOMNE 2026 — DÉPART LE 25 SEPTEMBRE 2026\n\n100 jours de pratique pour apprendre à :\n1. Cadrer un besoin réel et définir un objectif.\n2. Piloter l’IA avec des consignes claires.\n3. Produire des documents, des supports et des sites utiles.\n4. Tester, corriger et valider vos réalisations.\n5. Documenter votre travail dans un portfolio de preuves.\n6. Comprendre votre méthode pour savoir refaire et améliorer.\n\nCodex, VS Code, Git et GitHub sont au service de vos projets. L’IA assiste. L’humain pilote, contrôle et décide.\n\nQuel besoin de votre activité souhaitez-vous transformer en une première réalisation concrète ?\nDécouvrez la formation et présentez votre besoin :\n${urls.linkedin}\n\nProf. Abderrahman EL HISSE\nLAB-NUMÉRIQUE-IA\n\n#Challenge100Jours #DeveloppementModerne #ProfessionnelAugmente #Codex #LNIA`
};
for (const [channel, message] of Object.entries(messages)) {
  writeFileSync(new URL(`${channel}.md`, out), `# Publication ${channel} — brouillon à relire\n\n${message}\n`);
}
const csv = ['canal,utm_source,utm_medium,utm_campaign,utm_content,url', ...config.sources.map((source) => `${source},${source},${config.medium},${config.id},${config.content},"${urls[source]}"`)];
writeFileSync(new URL('liens-entrants.csv', out), csv.join('\n') + '\n');
writeFileSync(new URL('LIENS-ET-DIFFUSION.md', out), `# Campagne — liens et diffusion\n\nDestination effective : ${destination}\n\nStatut : ${config.announcementUrl ? 'URL d’annonce configurée ; vérifier sa publication réelle.' : 'Annonce non publiée. Les messages utilisent l’offre existante.'}\n\n| Canal | Lien d’entrée |\n|---|---|\n${config.sources.map((source) => `| ${source} | ${urls[source]} |`).join('\n')}\n\nLes UTM distinguent l’origine des clics. Aucun outil de mesure n’est installé dans ce pack : ils ne constituent pas des statistiques de fréquentation. Ne jamais y placer de données personnelles.\n\n1. Valider le message et choisir son affiche.\n2. Vérifier la destination dans le navigateur.\n3. Copier le texte dans votre propre compte ou groupe.\n4. Contrôler l’aperçu et publier vous-même.\n\nAucun compte Facebook ou LinkedIn n’a été inventé. Aucun message n’est envoyé automatiquement. Les boutons sociaux du site ouvrent seulement un composeur de partage.\n\nAprès publication humaine de l’annonce, renseigner announcementUrl dans config/campaign.json puis lancer npm run ci pour reconstruire le site et régénérer ces fichiers.\n`);
console.log(`Messages et liens des 3 canaux générés dans ${fileURLToPath(out)}`);
